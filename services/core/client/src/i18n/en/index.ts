import type { BaseTranslation } from "../i18n-types.js";

const en = {
    theme: {
        light: "Light",
        dark: "Dark",
        system: "System",
    },
} satisfies BaseTranslation;

export default en;
